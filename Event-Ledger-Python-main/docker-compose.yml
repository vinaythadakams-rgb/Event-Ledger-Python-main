version: "3.9"

services:
  event-ledger:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - ./event_ledger.db:/app/event_ledger.db
    environment:
      - PYTHONUNBUFFERED=1
